# GUIProjects

Swing GUI exercises.

## Compile

```powershell
javac -d out (Get-ChildItem -Path src -Recurse -Filter *.java).FullName
```

## Run SwingAccumulator

```powershell
java -cp out hust.soict.globalict.swing.SwingAccumulator
```
